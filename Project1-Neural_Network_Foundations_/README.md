this comprehensive lab, we'll build a neural network from the ground up, first using Python and NumPy to understand the mechanics, and then using the powerful frameworks of PyTorch and TensorFlow to see how it's done in practice.
🎯 Learning Objectives
By the end of this lab, you will:

Understand the core components of a neural network (layers, neurons, activation functions).
Implement a neural network from scratch using NumPy.
Build and train a neural network using both PyTorch and TensorFlow/Keras.
Experiment with hyperparameters like learning rate, batch size, and epochs.
Diagnose and address overfitting with techniques like dropout.
Compare and contrast PyTorch and TensorFlow
